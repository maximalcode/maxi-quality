# Node
node_modules/
*.tsbuildinfo

# .NET
bin/
obj/

# Rust — and Maven, which reuses the name
target/

# OS / editor
.DS_Store
*.swp

# Python bytecode — scripts/ is a package tree only by accident of imports
__pycache__/
*.pyc

# maxi-quality agent guard — per-checkout state, never committed
.claude/agent-guard-receipt.json
