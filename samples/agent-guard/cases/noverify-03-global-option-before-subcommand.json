{
  "why": "`git -c x=y commit` puts a value-taking global option in front of the subcommand. A tokenizer that assumes argv[1] is the subcommand sees `-c` and gives up, and the flag walks through.",
  "hook": "noverify",
  "setup": { "git": false },
  "event": {
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "tool_input": { "command": "git -c core.hooksPath=/dev/null commit --no-verify" }
  },
  "expect": {
    "decision": "deny",
    "reason_contains": ["--no-verify"]
  }
}
