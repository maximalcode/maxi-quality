{
  "why": "NEGATIVE CONTROL. The command this hook sees hundreds of times per adoption must produce nothing at all — not an allow decision, not a warning line, nothing.",
  "hook": "noverify",
  "setup": { "git": false },
  "event": {
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "tool_input": { "command": "git commit -m 'feat: a real message'" }
  },
  "expect": { "decision": "allow" }
}
