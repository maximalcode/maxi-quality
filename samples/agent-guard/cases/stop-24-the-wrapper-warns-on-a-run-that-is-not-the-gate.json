{
  "why": "The `--` form is legal for an ad-hoc run, and with a gate declared it produces a receipt the Stop hook will refuse. The useful place to say so is where the command was chosen, not at the end of the next turn — so the wrapper warns, and still records honestly rather than refusing to write.",
  "hook": "stop",
  "setup": {
    "committed": {
      "a.txt": "one\n"
    },
    "worktree": {
      "a.txt": "one\ntwo\n"
    },
    "config": {
      "gate_command": "true && true"
    },
    "record": {
      "command": [
        "true"
      ]
    }
  },
  "event": {
    "hook_event_name": "Stop"
  },
  "expect": {
    "decision": "block",
    "record_exit": 0,
    "record_stderr_contains": [
      "not the gate",
      "--gate"
    ],
    "reason_contains": [
      "declared gate"
    ]
  }
}
