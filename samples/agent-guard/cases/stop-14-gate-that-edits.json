{
  "why": "A FORMATTER IS A GATE THAT EDITS. The fingerprint is taken before the command runs, so a gate that rewrites files leaves a receipt that no longer matches and the next stop asks for another run. Hash it afterwards instead and a passing verdict gets recorded for content no tool ever read.",
  "hook": "stop",
  "setup": {
    "committed": {
      "a.txt": "one\n"
    },
    "worktree": {
      "a.txt": "one\ntwo\n"
    },
    "record": {
      "command": [
        "sh",
        "-c",
        "printf 'formatted\\n' >> a.txt"
      ]
    }
  },
  "event": {
    "hook_event_name": "Stop"
  },
  "expect": {
    "decision": "block",
    "record_exit": 0,
    "reason_contains": [
      "no longer exists"
    ]
  }
}
