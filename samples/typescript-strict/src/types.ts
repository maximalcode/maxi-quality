// Support file. Carries one type and one value so the fixtures below can
// exercise both halves of verbatimModuleSyntax. Produces no errors of its own.
export interface Widget {
  id: string;
}

export const WIDGET_KIND = 'widget';
